const Config = {
  API_KEY: process.env.API_KEY || null,
  NODE_ENV: process.env.NODE_ENV || null,
  NODEPORT: process.env.NODEPORT || 5000,
  PROJECT_ID: process.env.PROJECT_ID || null,
}

export default Config
